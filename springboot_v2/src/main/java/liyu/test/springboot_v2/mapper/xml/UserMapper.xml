<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="liyu.test.springboot_v2.model.User">
    <resultMap type="liyu.test.springboot_v2.model.User" id="BaseResultMap"></resultMap>
    <sql id="cols">
        password as password,username as username,role_id as roleId
    </sql>
    <sql id="whereSql">
        <where>
            <if test="roleId != null">
                and role_id=#{roleId}
            </if>
            <if test="password != null">
                and password=#{password}
            </if>
            <if test="username != null">
                and username=#{username}
            </if>
        </where>
    </sql>
    <insert id="insert">
        insert into t_user(password,username) values(#{password},#{username})
    </insert>
    <update id="merge">
        update t_user set password=#{password},username=#{username} where role_id=#{roleId}
    </update>
    <update id="dynamicUpdate">
        update ${tableName} set ${columnName}=#{value} where role_id = #{primaryKey}
    </update>
    <delete id="delete">
        delete from t_user where role_id=#{roleId}
    </delete>
    <delete id="deleteBatch" parameterType="java.util.List">
        <foreach collection="list" item="item" index="index" open="begin" close=";end;" separator=";">
            delete from t_user where role_id=#{item.roleId}
        </foreach>
    </delete>
    <select id="findone" resultMap="BaseResultMap">
        select <include refid="cols"/> from t_user where role_id=#{roleId}
    </select>
    <select id="findlist" resultMap="BaseResultMap">
        select <include refid="cols"/> from t_user <include refid="whereSql"/>
    </select>
    <select id="findcount" resultType="java.lang.Long">
        select count(1) from t_user <include refid="whereSql"/>
    </select>
</mapper>